/**
 * Configuration for topbar
 */

// The searchBar can have the following display options:
// - 'always': Show search on all pages
// - 'notLandingPage': Show search on all pages except landing page
// - 'onlySearchPage': Show search only on search page
// - 'none': Never show search (completely disable search)
export const topbar = {
  searchBar: {
    display: 'none',
  }
};